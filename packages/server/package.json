{
  "name": "@board-forge/server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "pnpm --filter @board-forge/shared build",
    "build": "tsc -p tsconfig.json",
    "prebuild": "pnpm --filter @board-forge/shared build",
    "dev": "tsx watch src/index.ts",
    "prestart": "pnpm --filter @board-forge/shared build",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@board-forge/shared": "workspace:*",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "@types/ws": "^8.18.1",
    "tsx": "^4.21.0",
    "typescript": "~6.0.2"
  }
}
